
name: Test Secrets Access

on: [workflow_dispatch]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: Check if secrets work
      run: |
        if [ -n "${{ secrets.TEST_SECRET }}" ]; then
          echo "Secrets are accessible!"
        else
          echo "No secrets found or permission issue"
        fi
